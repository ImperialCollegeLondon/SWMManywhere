## PR scope (HEAD vs `origin/main`) — maintainer-injected, truncated

**Changed paths (2):**
- .github/workflows/ci_template.yml
- .github/workflows/publish.yml

### Lockfile / manifest diff (dependency-relevant chunks only)
```diff
(no lockfile/manifest hunks in parsed diff)
```

### First non-lockfile diff hunks (context)
```diff
diff --git a/.github/workflows/ci_template.yml b/.github/workflows/ci_template.yml
index 7b92db8..dc07191 100644
--- a/.github/workflows/ci_template.yml
+++ b/.github/workflows/ci_template.yml
@@ -32,6 +32,9 @@ jobs:
     - name: Install dependencies
       run: pip install -e .[dev]
 
+    - name: Run pre-commit
+      run: pre-commit run --all-files
+
     - name: Run tests
       run: pytest

diff --git a/.github/workflows/publish.yml b/.github/workflows/publish.yml
index 24c3e5f..6cfcb0d 100644
--- a/.github/workflows/publish.yml
+++ b/.github/workflows/publish.yml
@@ -55,7 +55,7 @@ jobs:
 
     steps:
     - name: Download packages built by build-and-inspect-python-package
-      uses: actions/download-artifact@v5
+      uses: actions/download-artifact@v8
       with:
         name: Packages
         path: dist
@@ -79,7 +79,7 @@ jobs:
 
     steps:
     - name: Download packages built by build-and-inspect-python-package
-      uses: actions/download-artifact@v5
+      uses: actions/download-artifact@v8
       with:
         name: Packages
         path: dist
```